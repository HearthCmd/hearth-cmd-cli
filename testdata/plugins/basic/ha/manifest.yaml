plugin_slug: ha
display_name: Home Assistant
version: 0.1.0
manifest_schema: 1
description: Test fixture — minimal HA manifest.
executable: ./hearth-plugin-ha
verbs:
  - name: turn_on
    description: Turn on a light, switch, or scene.
    args:
      entity_id:
        type: string
        required: true
    output: json
